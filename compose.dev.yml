services:
  bot:
    image: ${IMAGE_NAME:-local/roulette-apps}:${IMAGE_TAG:-dev}
    build:
      context: .
      dockerfile: Dockerfile

  admin:
    image: ${IMAGE_NAME:-local/roulette-apps}:${IMAGE_TAG:-dev}
    build:
      context: .
      dockerfile: Dockerfile

  rotator:
    image: ${IMAGE_NAME:-local/roulette-apps}:${IMAGE_TAG:-dev}
    build:
      context: .
      dockerfile: Dockerfile
